ToBeDone：

1. C++ Easy GUI库

2. JavaScript 流程图编程



git for windows镜像下载：
https://github.com/waylau/git-for-win

git clone https://github.com/zhangyun007/Sword.git
git config --global user.email "zhangxp147@qq.com"
git config --global user.name "zhangyun007"
  
进入项目根目录下：
git add *
git commit -a
git push


编程相关软件：

Visual Studio C++ Community 2019:
https://visualstudio.microsoft.com/zh-hans/vs/features/cplusplus/
下载安装C++相关的开发软件，进入Developer Command Prompt for VS 2019命令行下，进入Math-physics目录下，运行nmake生成可执行文件test.exe，运行nmake clean清除生成的文件。

notepad++编辑器：https://notepad-plus-plus.org/
默认的风格可能不是很喜欢，点击“设置/语言格式设置”，然后在”选择主题“选项里选择自己喜爱的主题；点击视图/自动换行。


目录结构

JS			重点用于Canvas画图，我们尝试使用JavaScript建立各种作图工具。
Python3		数据可视化、网站框架(Tornado, Django)，使用stable版本：Python3.6.8。
Php			Wordpress，Drupal
Fly/		C++/STL程序，用visual studio c++(cl.exe)编译


C:\Windows\System32\drivers\etc\hosts文件，修改后能连上大部分国外网站：
https://raw.githubusercontent.com/racaljk/hosts/master/hosts

hosts文件末尾添加：
151.101.185.194 http://github.global.ssl.fastly.net 
192.30.253.112 http://github.com


程序设计语言的几个重要元素：


一、类型系统。
	
	静态类型还是动态类型？C/C++,Java是静态类型，变量需要先声明，再使用；Python, Bash, Php是动态类型，变量直接使用。
	
二、数组及复杂数据结构
	数组，链表，哈希数组，平衡树结构等等。C/C++复杂数据结构是通过标准模板库（STL）来实现；并不是语言核心，而Python Php等复杂数据结构则是语言核心。个人倾向将数据结构作为语言关键字实现，而不是作为标准库实现。
		
三、条件和循环语句。
	
	If for while 各个语言大同小异。 
		
四、函数

	函数是程序设计语言的重要元素。能否递归，能否把函数赋给变量使用，能否用无名函数？

五、结构体/类

	主要起封装作用。把数据和相关的函数封装在一起，便于修改和维护。父类和子类。 继承、多态。
	
六、自动内存回收？
	
	C/C++申请内存，使用完成后，需要自己释放。malloc/free new/delete。其他大多语言不需要手动回收。
	
七、泛型编程

	C++这类静态类型语言，应当有泛型编程元素，在C++里表现为模板template，有模板函数和类模板。