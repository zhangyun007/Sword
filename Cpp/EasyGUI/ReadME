一、排版系统应该从html/css中学习优秀的设计。

二、网上找到一个很好的JSON库，非常方便和STL容器互相转换，所以EasyGUI库的排版使用JSON格式。

三、以我的看法，html页面和json格式可能可以互相转换，也就是说，一个html文本完全可以改写成JSON格式，也许将来会出现能直接解析JSON格式直接显示页面的浏览器。

四、EasyGUI图形控件支持百分比定位和像素定位。



四种方式描述图形界面： json格式、S表达式()、XML、以及自创的GUI格式。